1： 2017.12.28 开始工程
app下载更新模块，更新不了首先看服务器返回的url是不是一个完整可用的路径;

TODO :打包必须修改地方：
   1:BaseUrl的替换。
   2:gradle文件中:版本号,内部版本号的递增
   3:检查Constant类中 BaseUrl是否为线上路径。
   兼职呗打包:(兼职呗开启混淆，广点通广告无法显示，建议关闭混淆)
    1: gradle文件中:applicationId的替换;极光推送的key替换; 签名文件配置的替换;
    2: Build--->Generate Signed Apk处：签名文件路径;账户名;key_alias的替换;
